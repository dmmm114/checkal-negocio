# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Segredos e ambiente — NUNCA versionar
.env
checkal/.env
deploy/polaris/agente.env

# Runtime local dos agentes (locks, PAUSA_LLM, saídas efémeras)
deploy/polaris/run/

# Dados locais / snapshots RNAL (podem conter dados pessoais — fora do git)
checkal/data/*
# exceção: lista de concelhos (sem dados pessoais; o varrimento nacional depende dela)
!checkal/data/concelhos.txt
*.db
*.sqlite3

# Caches de análise
**/rnal_cache/

# Estado transitório de ferramentas (mantém o conteúdo do brainstorm, ignora runtime)
.superpowers/**/state/
